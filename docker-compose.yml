services:
  dashboard:
    build: .
    ports:
      - "8501:8501"
    command: streamlit run app/streamlit_dashboard.py --server.address=0.0.0.0 --server.port=8501

  pipeline:
    build: .
    command: retail-kpi run-pipeline --mode full
